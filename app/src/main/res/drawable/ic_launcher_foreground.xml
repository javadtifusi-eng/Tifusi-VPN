<?xml version="1.0" encoding="utf-8"?>
<!-- Everything stays inside the 66dp adaptive-icon safe circle so round launcher masks never crop it. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/ic_logo_shield" />
    <item
        android:drawable="@drawable/logo_lamassu"
        android:left="36dp"
        android:top="31dp"
        android:width="36dp"
        android:height="27dp" />
</layer-list>
